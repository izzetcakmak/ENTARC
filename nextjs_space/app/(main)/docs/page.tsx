// Docs Page
// Plain-language guide to how ENTARC works

import { DocsContent } from './docs-content';

export const metadata = {
  title: 'How it works | ENTARC',
  description: 'A plain-language, interactive guide to the ENTARC autonomous venture agent.',
};

export default function DocsPage() {
  return <DocsContent />;
}
